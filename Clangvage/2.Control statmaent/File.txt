#include<stdio.h>
int main()
{
    int age;
    printf("Enter the age:");
    scanf("%d", &age);

    if(age >= 18)
    {
        printf("Candidate is eligible for vote");
    }
    else
    {
        printf("Candidate is not eligible for vote ");
    }

    return 0;
}
